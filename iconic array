#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin>>n;
  int a[n],i,c=0;
  for(i=0;i<n;i++){
      cin>>a[i];
      if(a[i]%(i+1)==0)
      c++;
  }
cout<<c;
return 0;
}
output
4
1 2 3 4
4
