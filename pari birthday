pari a girl is celebrating b'day so she keeps the cards in a line she wanted to know whether the sum of cards is greater than the age or not
#include <iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
  int n,k;
  cin>>n>>k;
  int a[n],i,sum=0;
  for(i=0;i<n;i++){
      cin>>a[i];
      sum+=a[i];
  }
  cout<<(sum>k);
  
return 0;
}
output
3 4
1 4 4
1
